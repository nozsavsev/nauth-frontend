import AccountLayout from "@/components/account/AccountLayout";
import EmailActionManager from "@/components/account/EmailActionManager";

const EmailActions = () => {
  return (
    <AccountLayout title="Email Actions">
      <EmailActionManager />
    </AccountLayout>
  );
};

export default EmailActions;
